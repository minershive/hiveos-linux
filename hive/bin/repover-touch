#!/usr/bin/env bash
# Saves repository version

repourl=`cat /etc/apt/sources.list.d/hiverepo.list | grep -m1 '^deb http' | awk '{print $2}'`
repover=`curl -s "$repourl/RepoVer"`
[[ ! -z $repover ]] &&
	echo "$repover" > /hive-config/RepoVer
